apiVersion: v1
kind: Service
metadata:
  name: tire-testing
spec:
  type: LoadBalancer
  selector:
    app: tire-testing
  ports:
    - name: app
      port: 8080
      targetPort: 8080
    - name: actuator
      port: 8081
      targetPort: 8081